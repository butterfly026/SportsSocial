import 'package:flutter/material.dart';

class BannerWorkPage extends StatelessWidget {
  const BannerWorkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Banner work Page"));
  }
}
